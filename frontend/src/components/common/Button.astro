---
interface Props {
  type?: "button" | "submit" | "reset";
  class?: string;
}

const { type = "button", class: className } = Astro.props;
---
<button type={type} class:list={["px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors", className]}>
  <slot />
</button>
